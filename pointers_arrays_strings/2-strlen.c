/*
 * File: 2-strlen.c
 * Auth: Leandro Pagani
 */

#include "main.h"

/**
* _strlen - Function that returns the length of a string
* @s: String
* Return: 0
*/
int _strlen(char *s)
{
	int length;

	while (s[length} != '\0');
	{
	length++;
	}

	return (length);
}
