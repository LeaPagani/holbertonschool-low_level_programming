#include "main.h"
/* more headers goes there */

/**
*mul - Write a function that multiplies two integers and returns the result
*@a: number to multiply
*@b: number to multiply
*Return: 0
*/
int mul(int a, int b)
{
	int mult;

	mult = (a * b);
	return (mult);
}
